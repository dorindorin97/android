<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <EditText
        android:id="@+id/fuzzUrlInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="72dp"
        android:layout_marginTop="8dp"
        android:hint="@string/http_fuzzer_url_hint"
        android:inputType="textUri"
        android:singleLine="true"
        android:textSize="13sp"
        android:fontFamily="monospace" />

    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/fuzzUrlInput"
        android:layout_above="@+id/fuzzDivider"
        android:divider="#E0E0E0"
        android:dividerHeight="1dp"
        android:padding="4dp" />

    <View
        android:id="@+id/fuzzDivider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_above="@+id/fuzzActivity"
        android:background="#E0E0E0" />

    <ProgressBar
        android:id="@+id/fuzzActivity"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_alignParentBottom="true"
        android:indeterminate="false"
        android:max="100"
        android:visibility="invisible" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fuzzToggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:layout_margin="8dp"
        android:src="@drawable/ic_play_arrow_24dp"
        app:backgroundTint="@color/app_color"
        app:fabSize="mini" />

</RelativeLayout>
